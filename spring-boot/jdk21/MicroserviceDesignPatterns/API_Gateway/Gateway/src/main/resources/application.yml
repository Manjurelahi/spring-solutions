server:
  port: 8085

spring:
  cloud:
    gateway:
      server:
        webmvc:
          routes:
          - id: DATE-SERVICE
            uri: http://localhost:9090
            predicates:
            - Path=/date/**
